import React from "react";
import BattleIcon from "../../../images/battle-back.svg";

const Battle = ({ width, height }) => {
  return  <BattleIcon width={width} height={height} />;
};

export default Battle;
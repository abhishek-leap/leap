import React from "react";
import  FilterShieldIcon from "../../images/filtered-shield.svg";

const FilterShield = ({ width, height }) => {
  return <FilterShieldIcon width={width} height={height} />;
};

export default FilterShield;